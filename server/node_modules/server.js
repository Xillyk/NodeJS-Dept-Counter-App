const express = require('express')
const bcrypt = require('bcrypt')
const mysql = require('mysql2')

const app = express()

app.use(express.json())

var db = mysql.createConnection({
  host: 'localhost',
  user: 'root',
  password: 'ILNVM1810;a',
  database: 'dept_counter_db',
  port: '3306'
})

db.connect((err) => {
  if (err) {
    console.log(err)
  } else
    console.log('database connected')
})


app.get('/user', (req, res) => {
  console.log('GET REQUEST')
  res.send('DATA to app')
})

// USER Register
app.post('/register', async (req, res) => {
  try {
    const hashedPassword = await bcrypt.hash(req.body.password, 10)
    // console.log(hashedPassword)
    var sql = 'INSERT INTO user VALUES (?, ?, ?)'
    var values = [req.body.username, hashedPassword, req.body.name]

    db.execute(sql, values, (err, results, fields) => {
      if (err) {
        console.log(err)
        res.status(500).send()

      } else {
        res.status(201).send('Success')
      }
    })
  } catch {
    res.status(500).send()
  }
})

// USER LOGIN
app.post('/login', (req, res) => {
  username = req.body.username
  password = req.body.password


  if (username == null) {
    return res.status(400).send('No user')
  }

  var sql = 'SELECT username FROM user WHERE username = ?'
  db.execute(sql, [username], (err, result) => {
    if (err) {
      console.log(err)
    } else {
      if (result == null || result == '') {
        return res.status(400).send('No user')
      }
      else {
        console.log('fetch username from db - success')
        var sql = 'SELECT password FROM user WHERE username = ?'

        db.execute(sql, [username], (err, result) => {
          if (err) {
            console.log(err)
          } else {
            console.log('fetch password from db - success')

            result = JSON.stringify(result)
            console.log('result = '+ result)
            result = result.split(':')
            result = result[1]
            result = result.substring(1, result.length - 3)
            console.log('new result = '+ result)
            
            hashedPasswordFromDB = result
            
            if (hashedPasswordFromDB != null) {
              if ( bcrypt.compareSync(password, hashedPasswordFromDB)) {
                console.log('success')
                res.send('Success')
              } else {
                console.log('login failed')
                res.send('Not Allowed')
              } 
            }
          }
        })
      }
    }
  })
})

app.listen(3000)

// var check = async (password, hashedPasswordFromDB) => {
//   if (hashedPasswordFromDB != null) {
//     console.log(password)
//     console.log(typeof hashedPasswordFromDB)
//     if (await bcrypt.compare(password, hashedPasswordFromDB)) {
//       return true
//     } else {
//       return false
//     } 
//   } 
// }
